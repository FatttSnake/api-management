<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.fatweb.apimanagement.mapper.system.ApiAccountMapper">
    <update id="deduct" >
        update t_s_api_account
        set balance     = balance - #{cost},
            update_time = utc_timestamp()
        where id = #{id}
          and balance >= #{cost}
          and deleted = 0
    </update>

    <update id="topUp" >
        update t_s_api_account
        set balance     = balance + #{amount},
            update_time = utc_timestamp()
        where id = #{id}
          and deleted = 0
    </update>
</mapper>
